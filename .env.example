# Didibood Price — environment templates
#
# Local dev:  cp .env.local.example .env       && ./scripts/run-local.sh
# Production: cp .env.production.example .env  (or use scripts/deploy-production.sh)
#
# Predict API works locally without DB if trained models exist under artifacts/models/.
# Train with refresh-data needs DATABASE_URL (recommended on server: direct 127.0.0.1:5432).
